"""The IPO analyser was removed when the product narrowed to public equities.

This file is a placeholder rather than a deletion because the project directory
rejects file removal (see paths.py). Delete it, along with
src/benchmark_pulse/ipo_sources.py, src/benchmark_pulse/ipo_score.py and
scripts/run_ipo.py, from Explorer when convenient. Nothing imports them.
"""
