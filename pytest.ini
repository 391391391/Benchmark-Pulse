[pytest]
pythonpath = src
testpaths = tests

# The project directory rejects new-file creation (see paths.py), so pytest
# cannot write .pytest_cache and the run ends in OSError even when every test
# passes. Nothing here depends on the cache.
addopts = -p no:cacheprovider
